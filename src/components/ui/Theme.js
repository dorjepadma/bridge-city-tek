import { createTheme } from '@mui/material/styles';



const theme = createTheme({
  palette: {
    primary: {
      main: '#417587',
    },
    secondary: {
      main: '#FFC107',
    },
  },
});



export default createTheme({theme});
